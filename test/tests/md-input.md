@module {function} some-module
@signature `doStuff()`

@body

This is the _beginning_ of the body.

```js
var DefineMap = require("can-define/map/map");
var connect = require("can-connect/all");

connect();
DefineMap.extend({

});
```

# Another section

Here I am.
